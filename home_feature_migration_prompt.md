# 防災支援システムのホーム画面移植用プロンプト

これはブラウザで動作するWeb版専用の実装指示です。FlaskなどのWebバックエンド、HTMLテンプレート、CSS、JavaScriptを使用し、デスクトップGUIアプリは作成しないでください。

以下の要件をすべて満たすように、現在のシステムへ防災支援アプリのホーム画面と、その周辺で必要な機能だけを実装してください。

既存システムの技術スタック、ルーティング、認証、データモデル、デザインシステムがある場合は、それらを優先して利用してください。既存機能を壊さず、ホーム画面と関連機能に限定して変更してください。実装後は、変更したファイル、起動方法、テスト結果、未実装または試験的な機能を報告してください。

## 1. 画面の目的

高齢者、災害時の利用者、外国人が、迷わず次の行動を取れる防災ホーム画面を作る。

最優先の操作は次の2つです。

1. 地域の防災情報を確認する
2. 避難所を確認し、避難所検索からナビゲーションを開始する

画面は情報を詰め込みすぎず、見出しとボタンを大きくし、認知負荷を下げる。

## 2. ホーム画面の構成

### 避難所検索画面との統一方針

このホーム画面は、既存または同時に移植する避難所検索画面と一貫したWebアプリとして実装してください。ホーム画面だけ別のデザインや地図仕様にしないでください。

- カラー、フォント、文字サイズ、角丸、枠線、影、ボタンの高さ、タブ、バッジ、余白を共通のデザイントークンまたはCSS変数で管理する。
- 高齢者向けの大きな文字、太字、十分なタップ領域、明確な赤色の危険表示を両画面で統一する。
- ヘッダー、言語切替、ユーザー情報、ログアウト、下部ナビゲーション、緊急モード切替の見た目と文言を統一する。
- 避難所検索画面に既に使いやすいLeaflet地図がある場合、その地図コンポーネント、初期位置、ズーム、マーカー、ポップアップ、ズーム操作をホーム画面でも再利用する。
- ホーム画面と避難所検索画面で別々の地図ライブラリ、別々の地図初期位置、別々の避難所データ形式を作らない。
- 共通化できる地図処理は、避難所データの描画、出発地マーカー、危険箇所マーカー、ポップアップ、検索結果との同期、地図サイズ再計算とする。
- 避難所検索画面の地図の方が情報量、操作性、正確性に優れている場合は、その実装を正式な共通地図として採用し、ホームのハザードマップまたは緊急モードにも同じ地図を表示する。
- ホームから避難所検索へ遷移したとき、検索条件や選択中の避難所など必要な状態を引き継げる設計にする。
- ホームのカード内地図は簡易表示でもよいが、地図タイル、帰属表示、マーカーの意味、データの信頼性表示を避難所検索画面と一致させる。

### 避難所検索画面の連携要件

避難所検索画面を移植または実装する場合は、次の仕様を満たし、ホーム画面と同じデータ・デザイン基盤を使用してください。

- 見出し、自宅住所入力、住所登録、現在地を使用、出発地状態を表示する。
- 施設名または住所の部分一致検索、リアルタイム絞り込み、検索結果と地図ピンの同期を行う。
- 避難所カードには施設名、住所、カテゴリ、混雑状況、実走行距離、所要時間を表示する。カード内に住所編集欄は置かない。
- 徒歩と車を切り替えられるようにし、利用するルーティングAPIの対応プロファイルを偽装しない。
- 避難所カードまたは地図ピンを選択したとき、出発地からの道路ネットワークに沿ったルートを青色で表示する。
- 直線距離ではなくルーティングAPIの距離・所要時間を表示する。
- ルート解除時は、現在の検索条件に合う避難所ピンとカードを復元する。
- 住所のジオコーディングに失敗した避難所は架空の座標で表示せず、位置未取得と明示する。
- ホームの緊急モードからは、登録済み避難所を確認でき、避難所検索画面で目的地選択とナビ開始を行えるようにする。

### ヘッダー

- サービス名を表示する。
- 右側に言語切替ボタンを配置する。
- ボタンは控えめなアウトライン形式にする。
- 表示は地球儀アイコンと `EN / JP`。
- ユーザー情報、権限ラベル、ログアウトボタンを配置する。
- ログアウトボタンは十分なサイズのボタンにし、ユーザー名や権限ラベルと重ならないようにする。
- 狭い画面では要素を折り返すか縦積みにする。

### 通常モード

画面中央に2カラムのカードを配置する。

左カラム:

- 見出し: `地域の防災情報`
- 状態表示: `平常`
- タブ:
  - `重要なお知らせ`
  - `イベント`
- 防災課などから登録された住民向けのお知らせを表示する。
- お知らせがない場合は、空の状態をわかりやすく表示する。

右カラム:

- 見出し: `マップ・交通情報`
- タブ:
  - `ハザードマップ`
  - `通行止め・道路状況`
- 地図をカード内に埋め込む。
- `現在地を登録・更新` ボタンを配置する。
- `周辺の地図を拡大` ボタンを配置する。

### 緊急モード

緊急モードは下部ナビゲーションなどから切り替えられるようにする。

- 赤色の緊急バナーを表示する。
- 災害発生と安全確保を大きく明示する。
- 緊急時の左カラムには、複数の避難操作ボタンを置かない。
- 最短経路や距離を実測できない場合、`1.2km` などの架空距離や経路を表示しない。
- 代わりに、登録済み避難所を地図上に表示する。
- 大きな主ボタン `避難所検索を開く` を配置し、避難所検索画面へ遷移させる。
- 「最寄り避難所へ自動ナビ開始」は、正確な現在地、避難所座標、経路計算が実装されている場合だけ提供する。
- 右側の地図には、登録済み避難所の名称と位置を表示する。
- 避難所データに緯度経度がない場合は、仮の位置を実在位置として表示しない。`試験データ` と明示するか、座標入力を必須にする。
- `周辺の通行止め情報` は補助的なアウトラインボタンにする。

### 下部ナビゲーション

以下の導線を提供する。

- ホーム
- 避難所検索
- 検索結果
- 管理者の場合は避難所登録
- 一般利用者の場合は使い方
- 緊急モード切替

## 3. 高齢者向けUI/UX

- 英語のサブタイトル、装飾的な英語ラベルは表示しない。
- 見出し、主要ボタン、タブは太字にする。
- 通常の本文より見出しとボタンを2〜4ポイント程度大きくする。
- 主要ボタンのタップ領域は十分な高さを確保する。
- 重要な操作は色、サイズ、位置で明確に区別する。
- 文字がボタンやカードからはみ出さないようにする。
- 色だけに依存せず、ラベルやアイコンも併用する。
- 通行止めなど危険情報は赤または黒で明確に表示する。

## 4. レスポンシブ対応

PC、タブレット、スマートフォンでレイアウトが崩れないようにする。

- 通常モードの2カラムは、画面幅が狭くなったら1カラムへ切り替える。
- 1カラム時は、上から次の順序にする。
  1. 地域の防災情報
  2. マップ・交通情報
- CSS Gridまたは既存フレームワークのレスポンシブ機能を利用する。
- 必要ならウィンドウ幅変更イベントを利用する。
- ヘッダーの言語ボタン、ユーザー情報、ログアウトボタンを重ならせない。
- 地図には固定または最小高さを設定し、サイズ変更後に再描画する。
- 下部ナビゲーションの文字とアイコンが狭い画面でも判読できるようにする。

## 5. 多言語対応

表示テキストをテンプレートやコンポーネントへ直接大量にハードコードしない。

次のような辞書型の翻訳データを作成する。

```text
translations = {
  "ja": {
    "home": "ホーム",
    "notice_title": "地域の防災情報",
    "map_title": "マップ・交通情報",
    "shelter_search": "避難所検索"
  },
  "en": {
    "home": "Home",
    "notice_title": "Local disaster information",
    "map_title": "Maps and traffic",
    "shelter_search": "Shelter search"
  }
}
```

- 日本語を初期言語にする。
- ヘッダーの言語ボタンを押すたびに日本語と英語を切り替える。
- 見出し、タブ、ボタン、アラート、ヘルプ、空状態、地図ラベルを切り替える。
- 地図マーカーのラベルとクリック後の詳細メッセージも切り替える。
- 利用者が登録した自由入力のお知らせや避難所名は、自動翻訳できない場合は原文表示とする。その場合、仕様として明記する。

## 6. 地図機能

ブラウザで動作する地図ライブラリを使う。Leafletなどを利用し、地図をカード内に埋め込む。TkinterなどのデスクトップGUI用ライブラリは使用しない。

- 初期位置は対象地域の実際の座標にする。
- 初期ズームは道路状況を確認できるレベル、目安として `14` にする。
- ズームイン/ズームアウト操作を有効にする。
- 地図の右下などにある標準ズーム操作が、高齢者でも操作しやすいよう十分な余白とサイズを持つようにする。
- OpenStreetMapなどの利用規約と帰属表示を守る。

### 通行止めマーカー

- ダミーデータを表示する場合、画面に必ず `試験データ` または `実際の通行規制とは異なります` と表示する。
- 公的機関や自治体のリアルタイムAPIに接続していない限り、実際の通行止め情報と表現しない。
- 2〜3箇所の赤または黒のマーカーを置く。
- マーカーラベルは日本語では `✖ 通行止め`、英語では `✖ Road closed` とする。
- マーカーをクリックしたら、地図下部またはツールチップに理由を表示する。
- 例:
  - `中央交差点付近：土砂崩れのため通行不可`
  - `本町交差点付近：道路冠水のため通行不可`
  - `港町交差点付近：落石のため通行不可`
- これらがダミー理由なら、その旨も明示する。

## 7. 避難所データ

避難所登録、検索、ホーム画面の地図表示で同じデータソースを使用する。

最低限、避難所は次の情報を持てるようにする。

```json
{
  "id": 1,
  "name": "避難所名",
  "latitude": 40.8244,
  "longitude": 140.7400,
  "address": "住所",
  "status": "開設中"
}
```

- 緯度経度が存在する避難所だけを正確な地図位置に表示する。
- 緯度経度がない既存データを地図上へ表示する場合は、仮配置と明記する。
- 避難所検索画面で、登録済み避難所の一覧、検索、詳細確認ができるようにする。
- 自動ナビゲーションを実装する場合は、現在地取得の許可、座標、経路API、エラー時の代替表示を用意する。

## 8. データの信頼性表示

画面上の情報を、次の3種類に区別する。

1. 実データ
   - 公的APIやデータベースから取得した情報
   - 取得時刻や更新日時を表示する

2. 登録データ
   - 管理者や運用担当者が登録した情報
   - 登録元や更新日時を表示できるようにする

3. 試験データ
   - UI確認用のダミー情報
   - 実際の災害、道路規制、避難経路と誤認させない表示を付ける

## 9. 完了条件

以下をすべて確認してから完了とする。

- ホーム画面が表示できる。
- ホーム画面と避難所検索画面で、主要な色、文字、ボタン、カード、タブ、ヘッダーが統一されている。
- もっとも操作性のよい地図実装が両画面で共通利用されている。
- ヘッダー上で言語を切り替えられる。
- ログアウトボタンが管理者ラベルに重ならない。
- スマートフォン幅で2カラムが1カラムになる。
- 緊急モードに架空の最短経路や架空距離が表示されない。
- 緊急モードから登録済み避難所の地図を確認できる。
- 緊急モードから避難所検索へ移動できる。
- ホームから避難所検索へ移動したとき、避難所データと必要な選択状態が矛盾なく引き継がれる。
- 通行止めマーカーをクリックすると理由が表示される。
- 通行止め情報が試験データの場合、その旨が画面に表示される。
- 地図のズーム操作が使える。
- JavaScript、テンプレート、バックエンドの構文エラーがない。
- 主要なルート、データ登録、検索、言語切替をテストする。
- 実データと仮データの範囲を最終報告に明記する。

## 10. 最終報告の形式

実装完了後、次の形式で報告する。

```text
変更したファイル:
- ...

実装した機能:
- ...

実データとして動作する機能:
- ...

試験的・仮データの機能:
- ...

実行方法:
- ...

検証結果:
- ...

残っている課題:
- ...
```
